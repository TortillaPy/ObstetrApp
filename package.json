{
  "name": "obstetrapp-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "ObstetrApp Backend",
    "ObstetrApp Frontend"
  ],
  "scripts": {
    "dev": "npx concurrently -k -p \"[{name}]\" -n \"backend,frontend\" -c \"blue,green\" \"npm run dev --workspace=\\\"ObstetrApp Backend\\\"\" \"npm run dev --workspace=\\\"ObstetrApp Frontend\\\"\"",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
